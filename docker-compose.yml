version: "3.9"

services:
  app:
    image: card-manager:local
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      NEXT_TELEMETRY_DISABLED: "1"
    ports:
      - "3000:3000"
    restart: unless-stopped
